import pickle
from preprocess import clean_text

def predict_news(text):
    model, tfidf = pickle.load(open("models/model.pkl", "rb"))

    text = clean_text(text)
    X = tfidf.transform([text])

    return model.predict(X)[0]